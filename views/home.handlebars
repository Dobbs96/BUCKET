  <nav>
    <div class="nav-wrapper blue lighten-5">
      <a href="/" class="black-text text-lighten-4 brand-logo center">BUCKET</a>
    </div>
  </nav>
 
 <div class="parallax-container">
      <div class="parallax"><img src="Parallax_IMGS/desert_blue.jpg"></div>
    </div>

<div class="container">
{{#each travels as |travel|}}
   <div class="row">
  <div class="col s12 m9">
    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <span class="card-title">{{location}} </span>
        <p>{{format_date starting_date}} to {{format_date ending_date}}</p>
        <p>Budget: ${{budget}}</p>
        <p>{{what_do}}</p>
        <p>written by {{user.username}}</p>

      </div>
      <div class="card-action">
        <a href="travel/{{id}}" id="comment" class="waves-effect waves-light btn" >Comment</a>
        <form class="col s12" >
        <div id="new-comment"></div>
        </form>
        {{#each comments as |comment|}}
        <div>
        {{!-- <p>{{user.username}}</p> --}}
        <p class="card-content white-text">{{comment.comment}}</p>
        </div>
        {{/each}}
      </div>
    </div>
  </div>
</div>
{{/each}}
{{#each recipes as |recipe|}}
<div class="row">
  <div class="col s12 m9">
    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <span class="card-title">{{name}}</span>
        <p>${{price}}</p>
        <p>{{link}}</p>
        <p>written by {{user.username}}</p>
      </div>
      <div class="card-action">
        <a href="recipe/{{id}}" id="comment" class="waves-effect waves-light btn" >Comment</a>
        <form class="col s12" >
        <div id="new-comment"></div>
        </form>
        {{#each comments as |comment|}}
        <div>
        {{!-- <p>{{user.username}}</p> --}}
        <p class="card-content white text">{{comment.comment}}</p>
        </div>
        {{/each}}
      </div>
    </div>
  </div>
</div>
{{/each}}
{{#each experiences as |experience|}}
<div class="row">
  <div class="col s12 m9">
    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <span class="card-title">{{what_do}}</span>
        <p>Budget: ${{budget}}</p>
        <p>Rate of going is {{rating}} out of 5</p>
        <p>written by {{user.username}}</p>
      </div>
      <div class="card-action">
        <a href="experience/{{id}}" id="comment" class="waves-effect waves-light btn" >Comment</a>
        <form class="col s12" >
        <div id="new-comment"></div>
        </form>
        {{#each comments as |comment|}}
        <div>
        {{!-- <p>{{user.username}}</p> --}}
        <p class="card-content white-text">{{comment.comment}}</p>
        </div>
        {{/each}}
      </div>      
    </div>
  </div>
</div>
{{/each}}
</div>

 <div class="parallax-container">
      <div class="parallax"><img src="Parallax_IMGS/volcano_blue_pink.jpg"></div>
    </div>


{{!-- Success modal, comment was added correctly --}}
  <!-- Modal Trigger -->
  {{!-- <a class="waves-effect waves-light btn modal-trigger" href="#modal1">Modal</a> --}}
  <!-- Modal Structure -->
  <div id="modal1" class="modal">
    <div class="modal-content">
      <h4>Success!</h4>
      <p>Your comment was added successfully!</p>
    </div>
    <div class="modal-footer">
      <a href="#!" class="modal-close waves-effect waves-green btn-flat">OK</a>
    </div>
  </div>
 </div>
 <div>


{{!-- Error modal, if comment was not added --}}
  {{!-- <button data-target="modal2" class="btn  red darken-1 modal-trigger">Modal</button> --}}
  <!-- Modal Structure -->
  <div id="modal2" class="modal">
    <div class="modal-content">
      <h4>Oops!</h4>
      <p>Something went wrong. Please try adding your comment again!</p>
    </div>
    <div class="modal-footer">
      <a href="#!" class="modal-close waves-effect waves-green btn-flat">OK</a>
    </div>
  </div>
 </div>
 <div>

